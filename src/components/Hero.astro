---
import Card from "./Card.astro";
---

<div class="hero-wrapper">
  <div class="hero-left">
    <Card>
      <h1 id="hero-title" class="hero-title" slot="title"></h1>
      <h2 id="hero-body" slot="body">
        Egresado de la Universidad de Guanajuato DICIS, soy un Ingeniero en
        Sistemas Computacionales ubicado en Guanajuato, México. Actualmente, me
        desempeño como Desarollador Web Full Stack Jr. creando productos
        eficaces y accesibles para el usuario.
      </h2>
    </Card>
  </div>
  <div class="hero-right">
    <Card sizeTitle="2rem" sizeBody="1.2rem">
      <h3 slot="title">Mis hobbies</h3>
      <h5 slot="body">
        A día de hoy, las cosas que más disfruto hacer son la lectura, los
        videojuegos, el fitness, la gastronomía, adquirir habilidades cotidianas
        y aprender sobre tecnología.
      </h5>
    </Card>
  </div>
</div>

<script>
  import TypeIt from "typeit/src/TypeIt";

  new TypeIt("#hero-title", {
    lifeLike: true,
  })
    .type("いらっしゃいませ.")
    .break()
    .type("Mi nomvre")
    .move(-2)
    .delete(1)
    .type("b")
    .move(null, { to: "END" })
    .type(" es Edwin.")
    .move(-20, {instant: true})
    .delete(10)
    .type("Willkommen.")
    .delete(11)
    .type("Welcome.")
    .delete(9)
    .type("Bienvenido.")
    .go();
</script>

<style>
  .hero-wrapper {
    z-index: 3;
    position: relative;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(max(15rem, 100%/2), 1fr));
    gap: 90px;
  }

  .hero-left {
    display: flex;
    flex-direction: column;
    grid-column: 1;
  }

  .hero-right {
    padding-top: 65%;
    display: flex;
    flex-direction: column;
    grid-column: 4;
  }

  @media screen and (max-width: 1400px) {
    .hero-wrapper .hero-right {
      padding-top: 0;
      grid-column: 1;
    }

    .hero-wrapper {
      gap: 20px;
    }
  }

  @media screen and (max-width: 800px) {
    .hero-title {
      font-size: 2.5rem;
    }
  }
</style>
